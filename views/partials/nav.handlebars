<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/"><span class="red">BBC</span> News</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        {{#if index}}<li class="nav-item active">{{/if}}{{#unless index}}<li class="nav-item">{{/unless}}
            <a class="nav-link" href="/">Home</a>
        </li>
        {{#if save}}<li class="nav-item active">{{/if}}{{#unless save}}<li class="nav-item">{{/unless}}
            <a class="nav-link" href="/saved">Saved Articles</a>
        </li>
        <li><button id="scrape" class="btn btn-primary">Scrape new articles</button></li>
        </ul>
    </div>
</nav>
